apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pms-dev

resources:
- ../../base

patches:
- patch: |-
    - op: replace
      path: /spec/rules/0/host
      value: "10.127.6.102"
  target:
    kind: Ingress
    name: pms-ingress
