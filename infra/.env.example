# 로컬 개발용 예시 환경변수 템플릿(.env)
POSTGRES_PASSWORD=pms
POSTGRES_USER=pms
POSTGRES_DB=pms
# DB (postgres container init 키)

DB_PASSWORD=pms
DB_USER=pms
DB_NAME=pms
DB_PORT=5432
DB_HOST=localhost
# DB (backend datasource 표준 키)

SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=local
# Spring

# - 실제 값이 들어있는 infra/.env 는 절대 커밋하지 않습니다.
# - 이 파일은 Git에 커밋합니다.

