# dev 환경 예시(실제 값은 서버의 보안 경로에 별도 저장)
# compose 실행 시: docker compose --env-file ./env/dev.env ...

# Spring profile
SPRING_PROFILES_ACTIVE_DEV=dev

# OIDC issuer (Resource Server)
# 예: https://dev-sso.example.com/realms/pms
OIDC_ISSUER_URI=https://CHANGE-ME

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8181

# DB
DB_PORT_DEV=5542
POSTGRES_DB_DEV=pms
POSTGRES_USER_DEV=pms
POSTGRES_PASSWORD_DEV=pms

# Ports
BACKEND_PORT_DEV=8180
FRONTEND_PORT_DEV=8181

