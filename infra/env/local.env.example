# local 환경 예시 (IDE에서 backend/frontend를 직접 실행, DB만 Docker)
# 설명: 이 파일은 docker-compose.local.yml과 함께 사용됩니다.

# ⚠️ IntelliJ 사용자 주의:
# IntelliJ는 .env 파일을 자동으로 읽지 않습니다!
#
# 두 가지 방법 중 선택:
# 1. EnvFile 플러그인 설치 후 프로젝트 루트의 .env.local 파일 사용
# 2. Run Configuration에서 Environment Variables 수동 설정
#
# 자세한 내용: docs/ENV_FILE_USAGE.md

# DB 설정 (docker-compose.local.yml용)
DB_PORT=5432
POSTGRES_DB=pms
POSTGRES_USER=pms
POSTGRES_PASSWORD=pms

# Backend 환경변수 (IntelliJ Run Configuration에서 사용)
# 프로젝트 루트의 .env.local 파일을 참조하거나
# 아래 내용을 IntelliJ Run Configuration에 복사하세요:
#
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=pms
# DB_USER=pms
# DB_PASSWORD=pms
# SERVER_PORT=8080
# SPRING_PROFILES_ACTIVE=local

