# SSO IdP 준비 전까지는 issuer-uri를 비워둠 (SecurityDevConfig가 활성화됨)
# SSO IdP 준비 후 환경변수로 설정: OIDC_ISSUER_URI=https://dev-sso.example.com/realms/pms
#spring:
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: ${OIDC_ISSUER_URI:}

# dev 환경에서 swagger 활성화 (SSO IdP 준비 전까지 개발 편의성 제공)
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
